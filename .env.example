# ==========================================
# 🚀 CLANKER TOKEN CONFIGURATION (v4.1)
# ==========================================
PRIVATE_KEY=
RPC_URL=https://mainnet.base.org

# --- 1. BASIC IDENTITY ---
# Your token's name and symbol
TOKEN_NAME="Alex Fin Global Enterprise"
TOKEN_SYMBOL="AFGE"

# The 0x address that will control the token (Liquidity Owner).
# If left empty, and REWARD_INTERFACE_ADMIN is set, it defaults to that address.
TOKEN_ADMIN=""

# Reward Configuration
REWARD_CREATOR="0xf13baF04ECA0E0Cbbdf844C1B095fA6871C92023"
REWARD_INTERFACE="0x2211d1D0020DAEA8039E46Cf1367962070d77DA9"
# Optional:
REWARD_CREATOR_ADMIN="0xf13baF04ECA0E0Cbbdf844C1B095fA6871C92023"
REWARD_INTERFACE_ADMIN="0x2211d1D0020DAEA8039E46Cf1367962070d77DA9"

# Request Vanity Address (Ends in ...B07)
# Set to "true" to enable Clanker-standard vanity suffix.
VANITY="true"

# --- 2. VERIFICATION & STRICT MODE ---
# Set to "true" to enforce Clankerworld's "Checklist" requirements (badge).
# This includes capping total fees to 5% and requiring description/socials.
STRICT_MODE="true"

# --- 3. TOKEN IMAGE ---
# Supports direct URLs (https://...) or IPFS CIDs (Qm..., bafy...)
TOKEN_IMAGE="ipfs://..."

# --- 4. SOCIALS & METADATA ---
# MANDATORY for Checklist Verification (if STRICT_MODE=true)
METADATA_DESCRIPTION="Detailed description of your project."
SOCIAL_X="https://x.com/yourproject"
SOCIAL_TELEGRAM=""
SOCIAL_FARCASTER=""
SOCIAL_WEBSITE=""

# --- 5. INDEXING CONTEXT ---
# STRICT_MODE REQUIRES Farcaster for reliable indexing.
CONTEXT_PLATFORM="farcaster"
CONTEXT_MESSAGE_ID="https://warpcast.com/..." # Warpcast URL or Cast Hash

# --- 6. FEES (v4.1 SDK SCHEMAS) ---
# Options: "static" or "dynamic"
FEE_TYPE="dynamic"

# STATIC FEES (100 bps = 1%)
FEE_CLANKER_BPS="500"  # Protocol (Recommend: 100)
FEE_PAIRED_BPS="500"   # LP (Recommend: 100)

# DYNAMIC FEES (Advanced)
FEE_DYNAMIC_BASE="100"    # Minimum fee (bps)
FEE_DYNAMIC_MAX="500"     # Maximum fee (bps)
# Volatility Controls (Defaults used if omitted)
# FEE_DYNAMIC_PERIOD="3600"
# FEE_DYNAMIC_RESET="86400"
# FEE_DYNAMIC_FILTER="100"
# FEE_DYNAMIC_CONTROL="100000"
# FEE_DYNAMIC_DECAY="9500"

# --- 6. SNIPER FEES (ANTI-BOT) --- 🔫
# Anti-snipe protection. Uses high initial fees that decay over time.
# Units are in "Unibps" (1,000,000 = 100%).
# Defaults if left empty: 66.67% start, 4.16% end, 15s decay.
SNIPER_STARTING_FEE="666777"   # 66.6777%
SNIPER_ENDING_FEE="41673"      # 4.1673%
SNIPER_SECONDS_TO_DECAY="15"   # 15 seconds

# --- 6. POOL CONFIGURATION ---
# Starting Tick: Determines initial price/mcap. 
# -230400 is approx $27k mcap (Standard Clanker).
# -223400 is approx $50k mcap (20 WETH Start).
# WARNING: Values like -400000+ are extremely low and cause "UNUSUAL_POSITIONS".
POOL_STARTING_TICK="-230400"

# Paired Token: "WETH" or a specific contract address
POOL_PAIRED_TOKEN="WETH"

# Pool Type (Preset): "Standard" (single position) or "Project" (staggered liquidity)
POOL_TYPE="Standard"

# Custom LP Positions (Advanced JSON): Overrides POOL_TYPE if set.
# Example: '[{"tickLower":-230400,"tickUpper":-212000,"positionBps":1000},{"tickLower":-212000,"tickUpper":887200,"positionBps":9000}]'
# POOL_POSITIONS_JSON=''

# --- 7. DEPLOYMENT OPTIONS ---
# Set to 'true' to simulate deployment without spending gas
DRY_RUN="false" 

# --- 8. DEV BUY (OPTIONAL) ---
# Amount of ETH to buy immediately upon deployment
DEV_BUY_ETH_AMOUNT="0" # e.g. "0.1"

# --- 9. CONTEXT (ADVANCED) ---
# For linking to specific Farcaster casts or external platforms.
# Clankerworld/GMGN displays this icon/source.
# Options: "farcaster", "twitter", "instagram", "clanker".
CONTEXT_PLATFORM="twitter"
CONTEXT_MESSAGE_ID="https://x.com/AlexFinn/status/2020178105579737440?s=20"
# CONTEXT_ID=""

# --- 10. REWARDS (ADVANCED) ---
# By default, 100% of fees go to TOKEN_ADMIN.
# Set REWARD_RECIPIENT to divert fees (Both Token & WETH) to a different wallet.
# This ensures strict separation of "Liquidity Owner" and "Fee Collector".
# Example: REWARD_RECIPIENT="0xFeeCollectorAddress"
# (If left empty, defaults to TOKEN_ADMIN)

# OPTION 2: Creator (99.9%) / Interface (0.1%) Split
# Set BOTH variables below to enable this mode.
# REWARD_CREATOR="0xCreatorAddress"
# REWARD_INTERFACE="0xInterfaceAddress"
# Optional: Separate Admin for each (Defaults to TOKEN_ADMIN if empty)
# REWARD_CREATOR_ADMIN="0xCreatorAdminAddress"
# REWARD_INTERFACE_ADMIN="0xInterfaceAdminAddress"

# You can override with JSON if needed (Rare):
# REWARDS_JSON='[{"recipient":"0x...", "admin":"0x...", "bps":10000, "token":"Both"}]'
